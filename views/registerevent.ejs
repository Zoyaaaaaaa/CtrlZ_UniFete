<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register for event</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:ital,opsz,wght@0,6..12,200..1000;1,6..12,200..1000&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Nunito Sans', sans-serif;
            margin: 0;
            padding: 0;
            background-color:#001D4A;
            overflow: hidden; 
        }
        
        form {
            max-width: 750px;
            margin: 100px auto 0; 
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 5px 4px 10px rgba(0, 0, 0, 0.6);
            animation: slideFromTop 0.5s forwards;
            max-height: 700px;
        }

        @keyframes slideFromTop {
            from {
                margin-top: -200px; /* Start off the screen */
            }
            to {
                margin-top: 200px; /* Move to desired position */
            }
        }
        
        form h2 {
            font-family: "Ubuntu", sans-serif;
            font-weight: 700;
            font-style: normal;
            font-size: 30px;
            text-align: center;
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 700;
            font-size: 18px;
        }
        
        input,
        select {
            width: 100%;
            padding: 10px;
            margin-bottom: 16px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 18px;
            transition: border-color 0.3s ease;
        }
        
        input:focus,
        select:focus {
            border-color: #001D4A;
        }
        
        select option {
            background-color: #fff;
            color: #001D4A;
        }
        
        select option:hover,
        select option:focus {
            background-color: #001D4A;
            color: #fff;
        }
        
        button {
            background-color: #001D4A;
            color: #fff;
            padding: 12px 25px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            display: block;
            margin: 20px auto 0;
            font-size: 18px;
            transition: background-color 0.3s ease;
        }
        
        button:hover {
            background-color: #006992;
        }

        @media screen and (max-width: 768px) {
            form {
                max-width: 90%;
            }
        }
        .home-icon {
            text-align: center;
            padding: 20px;
            font-size: 24px;
            color: #fff; /* Changed icon color */
            cursor: pointer;
            position: absolute;
            top: 20px;
            left: 20px;
            background-color: rgba(0, 0, 0, 0.5); /* Added background color */
            border-radius: 50%; /* Added border-radius */
            width: 40px; /* Added width */
            height: 40px; /* Added height */
            display: flex; /* Added display */
            align-items: center; /* Added align-items */
            justify-content: center; /* Added justify-content */
        }

        .home-icon a {
            color: #fff; /* Changed icon color */
            text-decoration: none; /* Removed underline */
        }
    </style>
</head>
<body>
    
    <form action="/studentemail" method="post">
        <h2>Registration Form</h2>

        <label for="YourName">Your Name:</label>
        <input type="text" id="YourName" name="YourName" required>

        <label for="name">Team Name:</label>
        <input type="text" id="name" name="name" required>

        <label for="email">Enter Email:</label>
        <input type="text" id="email" name="email" required>

        <label for="teammates">No. of Teammates:</label>
        <select id="teammates" name="teammates" required>
            <option value="Individual">Individual</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
        </select>

        <!-- Add the new dropdown field to select an event -->
        <label for="event">Select Event:</label>
        <select id="event" name="event" required>
            <!-- Populate options dynamically with events from the database -->
            <% events.forEach(event => { %>
                <option value="<%= event._id %>"><%= event.name %></option>
            <% }) %>
        </select>

        <button type="submit">Submit Request</button>
    </form>
    <div class="home-icon">
        <a href="/dashboard/student">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-house"
                viewBox="0 0 16 16">
                <path
                    d="M0 5.5l8-6.973 8 6.973v8.527a.5.5 0 0 1-1 0V8h-2v7a1 1 0 0 1-1 1h-1a1 1 0 0 1-1-1V9H5v7a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V5.5zm1.854-.353L8 1.793l6.146 3.354a.5.5 0 0 1 .18.684l-1.05 2.631a2 2 0
                    0 1 0 1 1.461 1.296l.365.037a1 1 0 0 0 .773.639l.895 2.238a.5.5 0 0 1 .952 0l.895-2.237a1 1 0 0 0 .773-.64l.365-.036a2 2 0 0 1 1.46-1.297l1.05-2.63a.5.5 0 0 1 .18-.684z" />
            </svg>
        </a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW"
        crossorigin="anonymous"></script>
</body>
</html>